server:
  port: 8761

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    enableSelfPreservation: false
spring:
  application:
    name: eureka-server

security:
  basic:
    enabled: true
  user:
      name: eureka # login username
      password: changeme
